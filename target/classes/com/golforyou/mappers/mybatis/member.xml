<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

		<select id="member_read" parameterType="member" resultType="member"> 	 
 	 		select * from GolforyouMemberNEW where username=#{username,jdbcType=VARCHAR} and mState=1
 	 	</select>
  	
 	 	<select id="id_check" parameterType="member" resultType="member">
 	 		select * from GolforyouMemberNEW where username=#{username,jdbcType=VARCHAR}
 	 	</select>
 	 	
 	 	<!-- 회원 정보 수정 -->
 	 	<update id="update_member" parameterType="member">
 	 		update GolforyouMemberNEW set mPhone=#{mPhone}, mAddr=#{mAddr}, mEmail=#{mEmail}, mFile=#{mFile} where username=#{username,jdbcType=VARCHAR}
 	 	</update>
 	 	
 	 	<update id="update_province">
 	 		update ranking set r_province=#{r_province} where r_id=#{r_id,jdbcType=VARCHAR}
 	 	</update>
 	 	
 	 	<!-- 회원 탈퇴 기능 -->
 	 	<update id="withdrawal">
 	 		update GolforyouMemberNEW set mDelcont=#{mDelcont}, mDeldate=sysdate, m_state=2 where username=#{username,jdbcType=VARCHAR}
 	 	</update>
 	 	
 	 	 <select id="getMember" resultType="member">
 	 		select * from GolforyouMemberNEW where username=#{username,jdbcType=VARCHAR}
 	 	</select>
 	 	
 	 	
</mapper>
